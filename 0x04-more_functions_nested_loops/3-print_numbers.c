#include "main.h"

/**
 * print_numbers - prints numbers between 0 to 9.
 * Return: no return
 */
int print_numbers(void)
{
	int ch;

	for (ch = 48; ch < 58; n++)
	{
		_putchar(ch);
	}
	_putchar('\n');
}
